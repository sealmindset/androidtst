# Android Test Harness Configuration
# Copy to .env and fill in values for your target app
#
# cp .env.example .env
# nano .env  # or your editor of choice

# Required - Target Android app package name
TARGET_PACKAGE=com.example.app

# Optional - Test account credentials (for apps requiring authentication)
# TEST_EMAIL=your-test-email@example.com
# TEST_PASSWORD=your-test-password

# Optional - Target identifier for testing (e.g., user ID, resource ID)
# Used by IDOR tests and similar security checks
# TARGET_ID=12345

# Optional - API base URL for backend testing
# API_BASE=https://api.example.com/v1
